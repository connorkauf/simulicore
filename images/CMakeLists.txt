set(IMAGES 
	envelope.png
	repo.png
	bug.png
	)

#-----------------------------------------------
# installation setup
#-----------------------------------------------
set(IMAGES_INSTALL images)

install(FILES ${IMAGES} DESTINATION ${IMAGES_INSTALL})
